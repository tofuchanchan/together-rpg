# 升级图修正

生成方式：内置 imagegen。输出：D-coop-upgrade-v2.png，页面使用此版本。

Edit the supplied two-player upgrade UI screenshot. Make ONLY this single exact text correction: at the very bottom center, the experience bar label currently says 'TEAM LV. 4'; change it to 'TEAM LV. 5', matching the existing level-up header above. Preserve every other pixel, all character positions, cards, P1/P2 panels, colors, composition, reference art style, every other word and number. Do not redesign anything. Output same full landscape image.
