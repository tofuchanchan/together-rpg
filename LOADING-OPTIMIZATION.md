# 资源加载优化验收

状态：本地完成，尚未推送或部署。

## 测量结果

同机 Chromium、新建浏览器上下文、禁用缓存、10 Mbps 下载限速、40 ms 延迟。
旧版取提交 `e10ce31bfda61e95a36c2576d3f8175245143010` 的文件，通过本地 HTTP 服务复现；新版使用工作区。
两组均为一次冷启动，时间不代表 GitHub Pages 或用户手机的真实耗时。

| 指标 | 优化前 | 优化后 |
| --- | ---: | ---: |
| 资源就绪前请求内容 | 75.2 MB | 20.5 MB，减少约 73% |
| 资源请求数 | 163 | 112 |
| 标题可用 | 5.6 秒 | 3.5 秒 |
| 开局资源就绪 | 62.5 秒 | 17.2 秒，减少约 73% |
| 全部转换图片的合计体积 | 72.8 MB | 41.0 MB |
| 商店图标及奖励怪图片 | 16.8 MB | 3.14 MB |

MB 为十进制。请求内容按 Resource Timing 的 decodedBodySize 统计，区别于含协议头的真实链路流量。
证据：`output/verification/loading-optimization/benchmark.json`、`initial-resources.json`。

## 实施内容

- 开局保留三职业、世界图集、构筑特效、通用怪物与宠物、选人肖像。
- 商店图标、装备、奖励怪、普通关卡目标、Boss 拆为五组后续资源。
- 第 2 关起预取关卡目标/奖励怪；第 4 关起预取商店/装备；第 8 关起预取 Boss。后台逐组加载，不在标题或选人时预取。
- 场景所需素材未就绪时等待再绘制，模拟时钟、伤害和事件倒计时冻结。失败后可重试或返回标题，支持键盘、鼠标及手柄 A/B。
- 保留原始 PNG，新增 82 张带内容哈希的 WebP 衍生图。除 21 个商店图标外，保持原尺寸、透明度和可见像素；生成工具逐图校验透明通道及黑白背景合成结果。
- 商店图标按原登记内容框裁切，长边最多 384 像素，保留外观 ID、源素材 SHA、原色与轮廓。
- 装备部件预处理按方向分批让出主线程。一次性处理产生约 1321 ms 帧间隔，修复后本地专项采样最大 61.5 ms。此结果不等于全程稳定 60 FPS 或手机性能保证。
- 保留已有图像解码重试逻辑；后续图片/清单增加超时与显式重试。

## 验证

- 91 项针对性单元/回归通过：资源、来源、商店、动画、图集、Boss/关卡、分辨率和新加载状态。最终微调后重跑受影响的 15 项，通过。
- 43 组浏览器检查通过：加载专项 8 组、标题/加载/Pages 子路径 16 组、冒险与 Boss 12 组、商店与奖励事件 7 组。
- 标准游戏客户端运行并查看截图/状态：实际键盘移动、闪避、选人及开局通过。
- 延迟 Boss 图片：等待期间时钟/血量不变，素材齐全后恢复。
- 中断奖励怪图片：显示重试、冻结事件时钟，重试后继续原场景。
- Boss 直达链接失败时，模拟手柄 B 可返回标题。
- 21 个图标、三职业选人、双人商店、Boss、巢穴截图已人工查看。
- 未运行全项目战斗平衡长测；实体手柄、手机性能、线上网络与缓存复访未测试。

## 复现与发布

- `node tools/verify-loading.mjs`
- `node tools/benchmark-loading.mjs`（使用 4176 端口，默认固定旧版提交）
- 图片构建：使用带 Pillow/WebP 的 Python 执行 `tools/optimize-runtime-art.py`；本机解释器为 `C:/Users/fuweicheng/.codex/skill-runtimes/python/Scripts/python.exe`。
- 冒险/商店浏览器工具显式准备其测试所需的后续资源，避免将确定性模拟帧数当作网络等待时间；正常游戏没有恢复全量预加载。
- 本轮没有增加 Service Worker、修改 Pages 缓存头、打包 JS 或继续拆分世界图集。图片哈希用于版本隔离。
- 发布时一并提交 `assets/runtime/`、生成映射、加载模块与引用文件；更新对象仅为静态站点。
