import fs from 'node:fs';
import assert from 'node:assert/strict';
import {chromium} from 'file:///C:/Users/fuweicheng/.codex/skills/node_modules/playwright/index.mjs';
const out='output/coop-verification/roguelite';fs.mkdirSync(out,{recursive:true});
const browser=await chromium.launch({headless:true}),p=await browser.newPage({viewport:{width:1440,height:940}}),errors=[],failed=[],checks=[];
p.on('pageerror',e=>errors.push(e.message));p.on('response',r=>{if(r.status()>=400)failed.push(r.url());});
const state=()=>p.evaluate(()=>JSON.parse(render_game_to_text())),advance=ms=>p.evaluate(ms=>advanceTime(ms),ms);
const shot=async name=>{await p.evaluate(()=>coopTest.render());await p.locator('#game canvas').screenshot({path:`${out}/${name}.png`});};
const tap=async key=>{await p.keyboard.down(key);await advance(30);await p.keyboard.up(key);await advance(20);};
const mark=s=>{checks.push(s);console.log('PASS',s);};
try{
 await p.goto(process.env.GAME_URL||'http://127.0.0.1:4173/');await p.waitForFunction(()=>window.assetsReady);await advance(0);await shot('menu');
 await p.evaluate(()=>{coopTest.view.click(580,216);coopTest.render();});await tap('Enter');assert.equal((await state()).humanCount,1);assert.deepEqual((await state()).heroes.map(h=>h.ai),[false]);await tap('q');assert.deepEqual((await state()).heroes[0].skills,[0,0]);await shot('solo-start');mark('Solo menu starts one human and no free AI; skill slots start empty');
 await p.evaluate(()=>{coopTest.router.slots[1]={type:'disconnected',id:99};});await advance(20);assert.equal((await state()).mode,'play');mark('Unused P2 device cannot pause solo mode');
 await p.evaluate(async()=>{const {enterShop,recruitShop,rerollShop}=await import('/src/coop/shop.js');const w=coopTest.world;w.room=5;w.mode='complete';w.heroes[0].gold=1000;enterShop(w);if(!recruitShop(w,0,w.shop.stalls[0].recruits[0].uid).ok)throw Error('First recruitment failed');rerollShop(w,0);if(!recruitShop(w,0,w.shop.stalls[0].recruits[0].uid).ok)throw Error('Second recruitment failed');w.leaveShop(0);w.room=1;w.mode='play';w.pressure=null;w.enemies=[w.createEnemy('goblin',0,-200)];w.heroes[1].x=450;w.heroes[1].y=300;w.heroes[2].x=500;w.heroes[2].y=250;});
 const aiBefore=(await state()).heroes.slice(1).map(h=>[h.x,h.y]);await advance(650);assert.ok((await state()).heroes.slice(1).every((h,i)=>Math.hypot(h.x-aiBefore[i][0],h.y-aiBefore[i][1])>1));mark('Two explicitly purchased AI move through real frame updates');
 await p.evaluate(()=>{coopTest.world.xp=coopTest.world.xpNext+2;});await advance(20);let s=await state();assert.equal(s.mode,'upgrade');assert.equal(s.rewardType,'attribute');assert.equal(s.xp,2);assert.deepEqual(s.ready,[false,true,true]);await shot('solo-attributes');await tap('e');assert.equal((await state()).mode,'play');mark('Mid-wave XP opens attribute choice; solo confirmation resumes immediately');
 await p.evaluate(()=>{const w=coopTest.world;w.enemies=[];w.spawnQueue=[];w.waveElapsed=w.waveDuration;w.waveTimer=1.11;});await advance(20);await shot('solo-skill');s=await state();assert.equal(s.rewardType,'skill');const unlock=s.offers[0].findIndex(o=>o.key.startsWith('active:'));await p.evaluate(i=>coopTest.world.choose(0,i),unlock);await tap('e');assert.equal((await state()).wave,2);assert.ok((await state()).heroes[0].skills.some(Boolean));mark('Wave reward unlocks a usable active and starts next wave');
 await p.evaluate(async()=>{const w=coopTest.world;coopTest.start(['warrior','mage'],1);const {createHero}=await import('/src/coop/recruitment.js');w.heroes.push(createHero('mage',1,{ai:true}));w.enemies=[];w.pressure=null;w.waveTimer=-100;const h=w.heroes[1];h.x=300;h.y=0;h.hp=20;w.pickups=[{id:901,type:'potion',x:370,y:0}];});await advance(800);assert.equal((await state()).pickups.length,0);assert.ok((await state()).heroes[1].hp>20);mark('Isolated pickup fixture: injured AI approaches and consumes a health potion');
 // Staged rendering fixtures isolate depth/anchor behavior from gameplay regressions.
 await p.evaluate(async()=>{const {ENEMIES}=await import(new URL('src/coop/enemies.js',location.href).href);coopTest.router.bind(1,{type:'keyboard',id:1});coopTest.start();const w=coopTest.world;w.effects=[];w.enemies=Object.keys(ENEMIES).map((kind,i)=>w.createEnemy(kind,-480+(i%5)*240,-250+Math.floor(i/5)*220));w.heroes.forEach((h,i)=>{h.x=-100+i*100;h.y=420;});w.camera={x:0,y:0,zoom:.9};w.options.shake=false;});assert.equal((await state()).enemies.length,13);await shot('thirteen-monsters');
 for(const type of ['spin','attack','bash'])for(const face of [0,2,4,6]){
  await p.evaluate(({type,face})=>{const w=coopTest.world;w.enemies=[];w.effects=[];w.hazards=[];w.projectiles=[];const h=w.heroes[0];h.x=0;h.y=0;h.face=face;h.skills=[1,1];h.action={type,t:.2,duration:.53,dir:{x:Math.cos(face*Math.PI/4),y:Math.sin(face*Math.PI/4)},hit:new Set()};w.heroes[1].x=450;w.camera={x:0,y:0,zoom:1};},{type,face});await shot(`warrior-${type}-${face}`);
 }mark('Ground spin and directional slash/bash render in four cardinal views');
 await p.evaluate(async()=>{const {skillPool}=await import(new URL('src/coop/builds.js',location.href));const w=coopTest.world,h=w.heroes[0];h.skills=[3,3];h.forms=[null,'bloodspin'];h.passives={rageEdge:2,battleRhythm:1};w.beginReward('skill');const pool=skillPool(h);w.offers[0]=[...pool.filter(o=>o.kind==='evolution'),...pool.filter(o=>o.kind!=='evolution')].slice(0,3);});await shot('evolution-offer');assert.ok((await state()).offers[0].some(o=>o.kind==='evolution'));mark('Staged eligible III + two complementary components show alternative evolution branches; no guaranteed random offer');
 await p.goto(new URL('art-lab.html',process.env.GAME_URL||'http://127.0.0.1:4173/').href);await p.waitForFunction(()=>window.artLab?.ready);await p.evaluate(()=>artLab.freeze(.3));await p.locator('#bestiary').screenshot({path:`${out}/bestiary.png`});assert.equal(await p.evaluate(()=>artLab.state.frames),192);assert.equal(await p.evaluate(()=>artLab.state.bestiarySpecies),11);assert.equal(await p.evaluate(()=>artLab.state.swarmSpecies),3);mark('Bestiary renders all three new swarm species and the original eight species in independent four-pose rows');
 assert.deepEqual(errors,[]);assert.deepEqual(failed,[]);fs.writeFileSync(`${out}/results.json`,JSON.stringify({checks,errors,failed,staged:['shop entry and gold to exercise real recruit transactions','potion healing scene','thirteen-monsters','warrior-*','evolution-offer'],scope:'Interaction/rendering and isolated AI pickup checks, not a natural-run balance result.',hardware:'Physical gamepads not tested'},null,2));
}finally{await browser.close();}
