# 纯手柄操作与闪避改键

本地实现完成，未提交、推送或部署。预览：http://127.0.0.1:4173/ 。

## 操作

| 场景 | 手柄操作 |
|---|---|
| 开始页、选人、暂停、结算 | 摇杆／方向键移动可见焦点，A 确认，B 返回 |
| 战斗 | 左摇杆移动，X／Y 技能，**LB／L1 闪避**，Start 暂停，View 图鉴 |
| 升级 | 上下选择，A 领取，Y 重掷，B 取消被动替换；具备双元素来源时 X 切换饲料 |
| 商店 | 上下选商品／招募／刷新／离店，左右切换穿戴者，A 确认，X 详情，Y 刷新；B 关闭详情或取消替换，在主页面选中离店但不直接确认 |
| 图鉴 | 方向键／摇杆导航筛选和条目，LB／RB 分类，A 查看，B 逐层返回；详情上下滚动、左右选择关联条目或重试按钮 |
| 设置、加载与异常 | 方向键选择，A 操作，B 返回；封面完全不可用时保留重试入口 |

标题页用手柄开始自动认领 P1；选人时第二只未绑定手柄按 A／Start 加入 P2。两位玩家独立选择职业、升级奖励和商店操作。设备断开暂停，重连按 A 主动认领原位置；更换设备可在暂停菜单重新绑定。误切键盘后，手柄仍可通过 Start 打开暂停自救。

界面沿用 Xbox 标准位置标识。PS 标准映射对应 A＝×、B＝○、X＝□、Y＝△、LB＝L1。键盘操作保留。原生全屏被浏览器拒绝时改用铺满可用窗口，可从同一菜单退出。

## 防误触

- 闪避与领取奖励分离：A 不再闪避，连续 LB 不会领取奖励。
- 绑定认领、页面切换与共享商店覆盖层消费触发按键，按住不会重复购买或确认。
- 双手柄同时操作共享图鉴时只执行一次翻页／返回；个人奖励仍可独立同时操作。
- 回到选人保留手动更换后的设备，不让原标题发起手柄抢回 P1。
- 修复 Phaser 和标题界面分别采样造成的竞态：返回标题的 A 不会再次触发开始。已固定采样顺序重现旧失败，并验证新实现。

## 验证结果

| 检查 | 结果 |
|---|---|
| 输入、双人操作、图鉴暂停、奖励串页、商店相关单测 | 81 项通过 |
| `verify-controller-flow.mjs` | 13 组纯手柄完整流程通过 |
| `verify-frontend-controller.mjs` | 8 组开始／设置／加载／异常／采样竞态检查通过 |
| `verify-codex-controller.mjs` | 13 组图鉴纯手柄检查通过 |
| 原开始页／可爱版布局 | 16／13 组通过 |
| 原图鉴／资源加载 | 16／8 组通过 |
| 原双人游戏／商店／升级串页 | 13／10／4 组通过 |
| 标准游戏客户端 | 进入战斗、移动、闪避通过；截图与状态已检查 |

纯手柄完整流程的所有用户动作通过模拟 `navigator.getGamepads` 发出，无鼠标或键盘代替导航。升级、商店和结算使用状态夹具缩短流程，不将其描述为自然通关；既有双人回归另保留 seed 17 的自然清房检查。

证据：`output/verification/controller-flow/`、`output/verification/frontend-controller/`、`output/verification/codex-controller/`。完整流程结果的页面异常与 HTTP 错误均为 0；桌面焦点、升级替换、商店替换、结算及图鉴手机尺寸截图已实际查看。`failure-state.json`／`failure.png` 和 `pre-fix-race.txt` 保留修复前复现，最终状态以各目录 `results.json` 为准。

## 未执行

- 实体 Xbox／PS 手柄、有线／蓝牙差异及实体手机验收。
- 浏览器首次音频解锁的纯手柄硬件验证；该行为仍受浏览器用户激活规则影响。
- 本次 GitHub Pages 发布与线上验证。
