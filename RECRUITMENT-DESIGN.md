# 商店候选队员

`recruitment.js` 只生成候选，不负责扣钱、加入队伍或替换成员。交易和稳定角色 ID 由商店处理；候选使用临时 ID `-1`，不会创建宠物、弹道、技能区域或特效。允许招募与已有队员相同的职业。

## 接口

- `HERO_ROLES`：与原战斗模型相同的三职业基础数据，供模型复用，避免两套数值。
- `createHero(role, id, {ai=false, level=1, name, rarity=1}={})`：新建完整英雄状态，嵌套数组/对象独立，装备槽为 `{weapon:null,armor:null}`。`level` 仅记录等级，本函数不会替调用方花升级点，避免重复成长。
- `rollRecruit(world, rng, uid)`：返回 `{uid,name,role,rarity,level,price,hero,attributePoints,buildPoints,attributePointsUsed,buildPointsUsed,attributeChoices,buildChoices}`。除显式 RNG 自身推进外，不修改世界。
- `recruitPrice(candidate)`：按候选等级、人才品质和实际携带装备计算费用。

## 等级与成长预算

等级取当前所有非 AI 玩家等级的平均值，四舍五入，最低 1。倒地玩家仍计入；AI 等级不计入。兼容旧存档/测试中缺少个人等级时的 `world.level`，没有真人时使用 1。

每候选有 `level - 1` 属性点、`floor((level - 1) × 0.65)` 构筑点。品质、房间数和装备品质都不增加点数。属性从当前合法属性池随机取得；已有构筑预算的前两点优先学习基础 Q/E，顺序随机、点数不足不免费补齐；之后每点抽一次正常三选一，优先选择与现有来源相容的技能/被动/核心/形态/进化，全部经 `applyReward` 应用。阶段参数仅使用构筑点序号，不能靠高房间给低等级候选跳阶段奖励。构筑抽到属性回退卡时不额外加属性，避免两个预算混用。

满四被动时只有显式合法替换才生效；替换不能让其余已持有组件失去来源。形态改变也重新检查旧组件，包含 III 级已满卡。如果三张随机卡都不能安全使用，就从剩余合法技能池继续抽到有效选择后消耗该点，精确花完预算，不补塞非法第五卡，也不升级超出 III。零构筑点的低等级候选没有主动；基础技能不保证流派成型或通用觉醒。`buildChoices` 保留每次提供的卡、阶段、替换记录，以及基础训练／补充合法池来源，支持回放检查。

人才普通/精良/稀有/传奇概率为 62%/25%/10%/3%。仅额外增加生命上限 `0/12/24/40`、普攻强度 `0/.03/.06/.10`、技能强度 `0/.03/.06/.10`，同类加算。不会加隐藏等级、技能或构筑点。

## 装备与收费

构筑与属性分配后，按当前房间为本职业独立生成一件武器、一件防具，经 `applyEquipment` 装备。候选初始化结束时满血；正式交易后换装本身不回血。后续正常属性升级需要通过 `withEquipmentBase` 暂时移开装备提供的封顶属性部分，升级后加回，防止装备让训练收益消失。

费用为 `12 + 等级 × 2 + 人才品质附加 [0,8,20,40]`，再加每件装备原价的 `25%/35%/60%/80%`（依装备品质，向上取整，至少该件回收价）。3 级普通人才带两件普通装备为 30 金，同装精良人才 38 金；双稀有装 106 金，双传奇装 198 金。高阶装备不是低价人才附赠，拆下转售也不会净赚金币。

定价最初参考 1000 种子的三人完整掉落预算：杀满前五关所有补怪预算时金币中位数 80，按 60% 拾取约 48。该数据来自人数缩放前的基准，不能直接套用当前单人/双人预算与单人金币补偿；不是自然通关收入，也不是首店实际可购买率。本版本据此让低阶人才与精良装备形成取舍，后续需用真实战斗收入调价格。

## 验证

`node --test tests/recruitment.test.mjs` 覆盖基础字段、嵌套状态隔离、真人平均等级、稀有度不增加预算、同职业、无世界副作用、两装备与价格、后续训练不丢失。另逐个回放 1000 种子、1–40 级候选的每次合法属性/构筑选择，检查实际花点、不超四被动、不超 III、来源与回收费用。候选生成测试不能替代商店 UI、招募交易与战斗 AI 的集成验证。
